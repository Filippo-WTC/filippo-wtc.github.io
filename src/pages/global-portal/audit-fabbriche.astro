---
// Redirect permanente: la pagina degli audit in fabbrica è stata eliminata
// (FIL-302), e il servizio non esiste più (decisione del committente,
// 01/09/2026). Il blocco «Servizi partner» che prima ospitava questo atterraggio
// è stato tolto dalla home di sezione: le ispezioni non sono più citate da
// nessuna parte, ed è voluto.
return Astro.redirect("/global-portal/come-funziona/", 301);
---
